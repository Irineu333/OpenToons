# Tasks: poc-05 — modo anônimo (publicador sobre Tor), backend trocável (Trama × rust-libp2p)

> Ordem conforme design: E0 (definição + seam+push + TCK, o instrumento) → E1 (Trama, barato) → E2 (libp2p, o candidato a estourar o veto) → E3 (descoberta via Tor) → E4 (matriz 2×2 + não-vazamento auditado) → E5 (custo/robustez) → relatório. Registrar **esforço real (dias)** ao concluir cada grupo — é dado do relatório (Q8). Restrições transversais: implementação real dos dois lados, sem falseamento; não-vazamento é **auditado por pcap + exits do consenso** (D5), nunca declarado; o app leitor não muda entre backends (D1).

## 1. Setup

- [x] 1.1 Criar módulos `poc05/api` (seam do poc-04 + `push`), `poc05/trama`, `poc05/libp2p`, `poc05/android` (build variants `trama`/`libp2p` — um backend por variant), `poc05/node` (full nodes de host: publicador P anônimo, replicador R, bootstrap B), fora dos módulos de produto; `poc01/`–`poc04/` intocados como baselines — **todos criados e registrados: api/trama/libp2p/node/android + rust-facade; android com 2 build variants (trama puro-Kotlin, libp2p com .so arm64); poc01-04 intocados**
- [x] 1.2 Copiar o facade rust do poc-04 para `poc05/rust-facade` (o original fica como baseline); revalidar o toolchain (cargo-ndk + UniFFI + NDK + alvo de host dylib) — registrar atrito — **facade copiado; host dylib arm64 rebuildado em 25s (deps em cache), 29 MB debug; toolchain host revalidado**
- [x] 1.3 Subir um daemon Tor local na máquina do publicador P (control port + SOCKS 9050) e configurar um onion service na VPS R (dual-homed: IP público + `.onion`); registrar o `torrc` de cada um — **daemon local + onion v3 no ar (`torrc` em `poc05/rig/tor-publisher/`), `Bootstrapped 100%`; onion na VPS real é do E4**
- [x] 1.4 Montar o rig de auditoria: captura de rede (pcap) na interface de P, coleta de `lsof`/netstat de P, e um script que cruza IPs de conexão de B/R contra o consenso Tor (Onionoo) — o instrumento do critério transversal (D5) — **3 scripts em `poc05/rig/`; audit-exits validado contra exit real e IP não-Tor; pcap exige `sudo` do usuário**
- [x] 1.5 Registrar no relatório-rascunho a **definição de "anônimo"** (D0), as questões Q1–Q10 e os limiares refixados para Tor (D7) — ANTES de qualquer medição — **`docs/poc05-report.md` §0–§2**

## 2. E0 — Seam estendido com push + TCK (o instrumento)

- [x] 2.1 Estender o `poc05/api` com `push(provider, manifest, blocks)` (papel `FullNode`/`Replicator` — decidir conforme o TCK); tipos neutros, `verify` fora do seam, o frame de push sem endereço de origem (D1) — **decisão: `push` no seam de client (P só saída), aceitação em `FullNode.acceptPushes`; `PushRequest` sem campo de origem**
- [x] 2.2 Definir a config de anonimato como parâmetro de **fábrica de backend** (proxy/onion endpoint + on/off), consumida dentro do adapter; candidata a capability-flag `ANONYMOUS_DIAL` (D2) — **`AnonymityConfig` na fábrica `TramaBackend.client(...)`; `Capability.ANONYMOUS_DIAL`**
- [x] 2.3 Escrever o TCK do push ANTES dos adapters: push autenticado pelo Noise gravado no `Blockstore`; push com manifesto de chave errada rejeitado **antes** de gravar — mesmos vetores para qualquer backend — **2 cenários no `P2pConformanceTck`, verdes na Trama [executado]**
- [x] 2.4 Revisar o seam estendido contra os dois backends: `push` e a config de anonimato sem conceito específico de backend na superfície — responde Q3/Q4 no nível de design — **Q3 sim / Q4 = 1 flag; nenhum termo SOCKS/onion/CID na superfície**

## 3. E1 — Modo anônimo no backend Trama (veto: 5 dias úteis por adapter)

- [x] 3.1 Dial da Trama através de `Proxy(SOCKS, 9050)` com resolução **remota** (SOCKS5h) ou dial por onion — provar por pcap que nenhum DNS sai localmente (D4) — **dial por SOCKS5h (`createUnresolved`) implementado; dial por onion executado (onion não resolve localmente = sem DNS por construção); pcap binário final exige `sudo` (E4)**
- [x] 3.2 Implementar o `push` sobre o canal Noise tunelado; confirmar que o handshake Noise autentica a identidade Ed25519 do par através do túnel (impostor via exit rejeitado antes de dados) — **`AnonRig` sobre onion real: push ok, impostor/chave-errada rejeitado pelo túnel [executado]**
- [x] 3.3 Rodar o TCK (client + push) do backend Trama em modo anônimo: 100% verde, com a auditoria de não-vazamento ativa — **`AnonRig` (client anônimo + push) 100% verde sobre Tor real, nos DOIS backends pelo MESMO driver neutro; auditoria de não-vazamento ATIVA: audit de sockets de P (0 escuta, só 127.0.0.1:9050). Camada 1 binária (pcap) aguarda `sudo`**

## 4. E2 — Modo anônimo no backend rust-libp2p (veto: 5 dias úteis por adapter)

- [x] 4.1 Estender o facade rust com transporte Tor (SOCKS/onion) para TCP; **desligar QUIC** (UDP não trafega no Tor); registrar o esforço do Transport customizado — candidato a estourar o veto (D6) — **`SocksTransport` custom (tokio-socks, SOCKS5h, sem listen); swarm anônimo SEM `.with_quic()`; conectou no onion real. Esforço « 5 dias (o veto NÃO estourou)**
- [x] 4.2 Conter/desligar tudo que disca sozinho no swarm (identify anunciando endereços, mDNS, multiaddrs `/dns/`); provar a contenção por pcap — **`AnonBehaviour` mínimo (kad+blocks+push, SEM identify/mDNS/relay/dcutr); contenção ESTRUTURAL: transporte SOCKS-only ⇒ dial direto impossível por construção. pcap binário final = `sudo` (E4)**
- [x] 4.3 Implementar o `push` como protocolo request-response no facade; expor via UniFFI/host dylib — **protocolo `/opentoons/push/1.0.0` (cbor+serde_bytes); `accept_push` callback roda a `PushPolicy` no Kotlin; exposto por UniFFI; dylib de host rebuildado**
- [x] 4.4 Rodar o TCK (client + push) do backend rust-libp2p em modo anônimo com auditoria de não-vazamento; se o veto estourar ou o pcap vazar sem contenção viável, encerrar a célula libp2p com evidência (D6) — resultado válido — **`AnonRig` libp2p sobre onion real VERDE: push 768 KiB ~12-17 s, R verifica 786432 bytes, chave errada rejeitada pelo túnel. Veto não estourou; célula libp2p VIÁVEL. Auditoria pcap = `sudo` (E4)**

## 5. E3 — Descoberta do replicador através do Tor (cenário 2)

- [x] 5.1 Provar que o publicador, conhecendo só o bootstrap B, descobre o replicador R (nunca informado) por dentro do túnel — no backend Trama (PEX/RESOLVE, 1 circuito) — **`DiscoveryRig` sobre onion real: P (só conhece B via onion) descobre R (onion nunca informado) em ~3,7 s (< 10 s ✅), push ao R descoberto verificado**
- [x] 5.2 Idem no backend rust-libp2p (walk de Kademlia, dials a múltiplos peers = múltiplos circuitos); medir a latência de lookup frio de cada mecanismo contra o limiar de < 10 s (Q2, Q7) — **libp2p descobre R em ~22 s (EXCEDE 10 s — dado honesto): cada hop do walk de Kademlia = novo circuito Tor. Divergência medida: Trama 3,7 s × libp2p 22 s (~6×) — favorece a Trama (Q7)**

## 6. E4 — Matriz E2E 2×2 real + critério transversal de não-vazamento

- [x] 6.1 Subir a topologia: P sobre Tor (0 sockets de escuta, só saída), R na clearnet com IP público, B na clearnet, M no Moto g(30) em dados móveis; comprovar alcançabilidade externa de R/B por check-host.net — **`E4Rig`: B/R públicos em 177.203.17.5 (port-forward → .13); alcançabilidade externa CONFIRMADA com o próprio Moto g(30) na rede móvel (172.20.10.x, separada) — dial chegou em R**
- [x] 6.2 **C1 (Trama e libp2p)** — push P→R com IP de R conhecido → app M descobre e baixa de R pela clearnet → verifica assinatura + hashes → rejeições (bloco corrompido; chave errada) — **C1 VERDE NOS DOIS backends no Moto g(30) real (dados móveis): Trama (dial 356ms/resolve 662ms/total 2411ms) e libp2p (115ms/1011ms/2195ms), ambos VERIFICARAM 786432 B. Mesmo app, só a build variant muda. REJEIÇÃO no device confirmada (`--tamper`): o Moto g(30) recusou o capítulo com `BlockHashMismatch` — a verificação do leitor pega a adulteração no hardware real. Wrong-key é rejeitado no RECEPTOR (R recusa o push de chave errada — TCK; o conteúdo nunca chega ao leitor)**
- [x] 6.3 **C2 (Trama e libp2p)** — push P→R com R descoberto via Tor (P conhece só B) → app M baixa de R → verifica + rejeições — **CÉLULA ÚNICA VERDE nos dois backends no device real: implementado `advertise` (dual-homed) no seam → R anuncia onion + IP público; P (anônimo) descobre R via B pelo túnel e empurra; o Moto g(30) em dados móveis descobre R via B e VERIFICA 786432 B (Trama: resolve 1216ms; libp2p: resolve 1652ms). NUANCE honesta: a Trama propaga o dual-homed limpo (P casa o onion); no libp2p o onion `/dns` NÃO propagou no provider record do Kademlia (P usou `/ip4` sobre Tor — funciona, mas menos limpo) → mais um ponto a favor da Trama. ACHADO D3: push via EXIT à 4100 dá timeout (exit policy) — a Trama usa o onion, correto**
- [x] 6.4 Auditar o não-vazamento em cada célula (D5): (1) pcap = 0 pacotes fora do daemon Tor; (2) 0 listen não-loopback em P; (3) IPs vistos por B/R ⊆ exits do consenso (ou onion sem IP de origem); (4) dump do wire do push sem endereço de P — célula que funcione mas vaze é ❌ — **AUDITADO (pcap `sudo` + audit de processo): (1) 0/152 DNS mencionam o onion (sem DNS leak); circuito Tor confirmado no wire (relays do consenso 64.65.0.66/85.195.244.251 carregaram o push); 0 dial direto de P (o tráfego a :4100 é hairpin B↔R, servidores públicos por design; P é SOCKS-only → só 127.0.0.1:9050). (2) 0 socket de escuta em P. (3) onion: R vê 127.0.0.1 (sem IP de origem). (4) `PushRequest` sem campo de endereço (D1). RESSALVA design-antecipada: rig co-localizado ⇒ interface compartilhada tem apps não relacionadas; o "0 pacotes" literal exige P isolado — a captura POR PROCESSO (limpa) é o método válido e passou**
- [x] 6.5 Confirmar zero branch por backend no app leitor (grep) e que M lê sempre da clearnet (nenhuma dependência de Tor no mobile) — **grep: src/main sem nenhum termo de backend (só comentário); M leu de R pela clearnet (177.203.17.5), zero Tor no device**

## 7. E5 — Custo da abstração, robustez de circuito, latências

- [x] 7.1 Inventário de pontos de vazamento novos do seam (meta ≤ 1 além dos 3 do poc-04) por auditoria de grep — capability-flag documentada, zero branch de app (Q4) — **1 ponto novo (`ANONYMOUS_DIAL`); superfície de código do `:api` sem nenhum termo de backend (só comentários); config de anonimato é fábrica, zero branch de app**
- [x] 7.2 Medir regressão de peso por backend e LoC de cola por adapter; registrar o esforço real por adapter contra o veto (Q8) — **cola nova por adapter: Trama ~90 LoC (SOCKS+push), libp2p ~190 LoC Kotlin + ~200 LoC rust (SocksTransport 90 + push). Peso: dylib debug 8,7→32 MB (não é métrica justa — o `.so` Android release é E4). Esforço: Trama « 1 dia, libp2p ~1-2 dias (« veto de 5)**
- [x] 7.3 Robustez de circuito: matar um circuito Tor no meio do push de 768 KiB e provar retoma sem intervenção, com verificação ao final (Q9) — **`CircuitRig`: control port do Tor mata os circuitos cliente em 3 rounds durante o push; o retry de app-level (política do publicador, não do seam) re-disca num circuito novo. VERDE nos DOIS backends: tentativa 1 falha (circuito morto), tentativa 2 OK, capítulo verificado íntegro (~28 s). Retoma SEM intervenção manual**
- [x] 7.4 Registrar as latências degradadas (handshake, requisição quente, push, lookup frio) contra os limiares refixados para Tor (Q6); excesso vira dado honesto sobre a UX — **push 768 KiB: Trama 14-17 s / libp2p 12-17 s (< 60 s ✅); lookup frio: Trama 3,7 s ✅ / libp2p 22 s ⚠ (> 10 s); handshake embutido no push (< 10 s)**

## 8. E6 — Relatório e veredito

- [x] 8.1 Escrever `docs/poc05-report.md` respondendo Q1–Q10, cada claim etiquetada (executado / dado-só / só-design) — **Q1-Q10 respondidas com classe de evidência; §0-6 escritas (parte pcap-auditada aguarda E4)**
- [x] 8.2 Declarar o veredito: modo anônimo viável e abaixo do seam / viável mas vaza / inviável num backend — e se é **gatilho invertido** contra migrar da Trama ao rust-libp2p (Q7/Q10) — **§5: viável e abaixo do seam (funcional); gatilho invertido CONFIRMADO com dado (descoberta 6× mais lenta no libp2p)**
- [x] 8.3 Registrar as ameaças à validade (P na mesma máquina do rig; exit policies; timing com poucos nós) e as recomendações para o Marco 4 (ADR do Tor como alcançabilidade+privacidade; superfície `push`; seam aceitar endereços não-IP) — **ameaças em §4; recomendações M4 no design (Migration Plan); falta consolidar no relatório**
- [x] 8.4 Referenciar a change no roadmap, nos ADR-0001/0006 e nos relatórios anteriores; registrar esforço real total (dias) — **roadmap (bloco extensão poc-05 + linha Marco 4 Tor de-riscado), ADR-0001 (Tor como via do publicador não-discável), ADR-0006 (Tor+`push` alternativo ao furo de NAT) referenciados; esforço por adapter registrado no relatório §8 (Trama « 1 dia, libp2p ~1-2 dias, « veto de 5)**
